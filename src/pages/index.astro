---
import BaseLayout from "@layouts/BaseLayout.astro";
import FloatingBlock from "@components/FloatingBlock.astro";
import Header from "@components/Header.astro";
---

<BaseLayout background="black">
  <Header color="black" />
  <FloatingBlock id="keith_dancers" offsetXDesktop={-10} priorityLoad />
  <FloatingBlock id="holiness" offsetXDesktop={20} />
  <FloatingBlock id="holiness2" offsetXDesktop={-5} />
  <FloatingBlock id="dragonrain" offsetXDesktop={15} />
  <FloatingBlock id="relief" offsetXDesktop={-15} />
  <FloatingBlock id="count" offsetXDesktop={5} />
  <FloatingBlock id="scenery" offsetXDesktop={25} />
  <FloatingBlock id="momentmat" offsetXDesktop={-20} />
  <!-- {Object.entries(categories).map(([category, workIds]) => (
    <WorkCollection title={category} workIds={workIds} />
  ))} -->
</BaseLayout>

<style>
  .gallery-section {
    margin-top: 10rem;
  }
</style>