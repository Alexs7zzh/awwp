  <a href="/" class="home">Akita Wakira</a>

  <header>
    <nav>
      <a href="/works">Works</a>
      <a href="/about">About</a>
    </nav>
  </header>

<style>
      a {
      text-decoration: none;
      transition: opacity 0.2s ease-in-out;
    }

    a:hover {
      opacity: 0.7;
    }

    /* --- Fixed Brand Name (Top Left) --- */
    .home {
      position: fixed;
      top: 2rem;
      left: 2rem;
      z-index: 1000; /* Ensures it stays on top of other content */
      
      font-size: 1.5rem;
      font-weight: 300; /* Heavier weight for presence */
      letter-spacing: 0.04em;
      
      /* Magic for contrast on any background */
      color: white; 
      mix-blend-mode: difference;
    }

    header nav {
      display: flex;
      justify-content: flex-end; /* Pushes links to the right */
      align-items: center;
      padding-top: 2.5rem;
      padding-right: 2rem;
      gap: 1.5rem; /* The space between "Works" and "About" */
    }

    header nav a {
      color: #525252;
      font-size: 1rem;
      font-weight: 300; /* Use a bolder weight for clarity, but the thin font-family keeps it elegant */
    }

  @media (max-width: 600px) {
      .home {
        top: 1.5rem;
        left: 1rem;
        font-size: 1.2rem;
      }
      header nav {
        padding: 1.5rem 1rem;
        gap: 1.5rem;
      }
      header nav a {
        font-size: 1rem;
      }
    }
</style>
